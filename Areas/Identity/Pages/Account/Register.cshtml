@page
@model PortfolioMVC.Areas.Identity.Pages.Account.RegisterModel
@{
    ViewData["Title"] = "Register";
}

<style>
    body {
        background: linear-gradient(135deg, #121212, #1e1e2f);
    }

    .auth-card {
        background: linear-gradient(145deg, #1e1e2f, #2a2a3d);
        border-radius: 16px;
        padding: 30px;
        box-shadow: 0 10px 40px rgba(0,0,0,0.6);
        color: #fff;
        transition: all 0.3s ease;
    }

        .auth-card:hover {
            transform: translateY(-5px);
        }

    .custom-input {
        background-color: #2a2a3d;
        border: 1px solid #444;
        color: #fff;
        border-radius: 8px;
    }

        .custom-input:focus {
            border-color: #0d6efd;
            box-shadow: 0 0 0 0.15rem rgba(13,110,253,.25);
            background-color: #2a2a3d;
            color: #fff;
        }

    .form-label {
        font-weight: 500;
    }

    .divider {
        border-top: 1px solid #444;
        margin: 20px 0;
    }

    .extra-links a {
        text-decoration: none;
    }

        .extra-links a:hover {
            text-decoration: underline;
        }
</style>

<div class="container d-flex justify-content-center align-items-center" style="min-height:80vh;">
    <div class="col-md-4">

        <div class="auth-card">

            <!-- TITLE -->
            <h3 class="text-center fw-bold mb-4">
                <i class="bi bi-person-plus"></i> Create Account
            </h3>

            <!-- FORM -->
            <form method="post">

                <div class="mb-3">
                    <label asp-for="Input.Email" class="form-label"></label>
                    <input asp-for="Input.Email"
                           class="form-control custom-input"
                           placeholder="Enter your email" />
                    <span asp-validation-for="Input.Email" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Input.Password" class="form-label"></label>
                    <input asp-for="Input.Password"
                           type="password"
                           class="form-control custom-input"
                           placeholder="Create password" />
                    <span asp-validation-for="Input.Password" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Input.ConfirmPassword" class="form-label"></label>
                    <input asp-for="Input.ConfirmPassword"
                           type="password"
                           class="form-control custom-input"
                           placeholder="Confirm password" />
                    <span asp-validation-for="Input.ConfirmPassword" class="text-danger small"></span>
                </div>

                <button type="submit" class="btn btn-success w-100">
                    <i class="bi bi-check-circle"></i> Register
                </button>

            </form>

            <!-- DIVIDER -->
            <div class="divider"></div>

            <!-- EXTRA LINKS -->
            <div class="text-center extra-links">

                <p class="text-light mb-1">Already have an account?</p>

                <a asp-page="./Login" class="text-info fw-semibold">
                    Login here
                </a>

            </div>

        </div>

    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}